<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Wiki Flower Search 🌸</title>

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: "Segoe UI", sans-serif;
    }

    body {
      background: linear-gradient(120deg, #ffe4e1, #fff0f5, #fce4ec);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 40px 20px;
    }

    .container {
      background: #fff;
      padding: 60px 40px;
      border-radius: 30px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
      text-align: center;
      width: 100%;
      max-width: 700px;
      transition: all 0.3s ease-in-out;
    }

    .flower {
      width: 100px;
      margin-bottom: 30px;
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }

    h1 {
      color: #e91e63;
      font-size: 2.5rem;
      margin-bottom: 25px;
    }

    form {
      margin-bottom: 30px;
    }

    input[type="text"] {
      padding: 15px 20px;
      width: 90%;
      max-width: 500px;
      border: 2px solid #e91e63;
      border-radius: 15px;
      font-size: 18px;
      margin-bottom: 20px;
      outline: none;
      transition: 0.3s;
    }

    input[type="text"]:focus {
      border-color: #d81b60;
      box-shadow: 0 0 10px rgba(233, 30, 99, 0.2);
    }

    input[type="submit"] {
      padding: 14px 30px;
      background-color: #e91e63;
      color: white;
      border: none;
      border-radius: 15px;
      font-size: 18px;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.2s;
    }

    input[type="submit"]:hover {
      background-color: #d81b60;
      transform: scale(1.05);
    }

    .buttons {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 10px;
      margin-top: 20px;
    }

    .button {
      padding: 12px 20px;
      border: none;
      border-radius: 12px;
      font-size: 16px;
      color: white;
      cursor: pointer;
      transition: transform 0.2s ease-in-out;
    }

    .button:hover {
      transform: scale(1.08);
    }

    .green { background-color: #4caf50; }
    .blue { background-color: #2196f3; }
    .red { background-color: #f44336; }
    .gray { background-color: #e0e0e0; color: black; }
    .black { background-color: #333; }

    @media (max-width: 600px) {
      h1 {
        font-size: 2rem;
      }

      input[type="text"] {
        width: 100%;
      }

      .buttons {
        flex-direction: column;
        align-items: center;
      }

      .button {
        width: 80%;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <img src="/images/flower.png" alt="Flower" class="flower"/>
    <h1>🌼 Wiki Flower Search 🌼</h1>

    <form action="/index" method="GET">
      <input type="text" name="person" placeholder="Enter the name of a flower ..." required />
      <br/>
      <input type="submit" value="Search 🌸" />
    </form>

    <div class="buttons">
      <button class="button green">Lily</button>
      <button class="button blue">Jasmine</button>
      <button class="button red">Rose</button>
      <button class="button gray">Lotus</button>
      <button class="button black">Sun Flower</button>
    </div>
  </div>
</body>
</html>
